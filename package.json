{
  "name": "polygram",
  "private": true,
  "version": "1.0.0",
  "description": "AI Prediction Market on TON",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "vitest",
    "lint": "pnpm -r lint",
    "knip": "knip"
  },
  "devDependencies": {
    "knip": "^5.0.0",
    "typescript": "^5.7.3",
    "vitest": "4.0.18"
  }
}
